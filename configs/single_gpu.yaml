# Inherits from default.yaml; single-node single-GPU (no torchrun/DDP).
# Usage: sbatch submit_train_singlegpu.slurm  OR  python train.py --config configs/single_gpu.yaml

distributed: false
sync_bn: false
